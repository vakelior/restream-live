name: Pokemon Live Stream

on:
  workflow_dispatch:

jobs:
  stream:
    runs-on: ubuntu-latest
    timeout-minutes: 350
    steps:
      - name: Install ffmpeg (PPA official)
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq software-properties-common
          sudo add-apt-repository -y ppa:ubuntuhandbook1/ffmpeg7
          sudo apt-get update -qq
          sudo apt-get install -y -qq ffmpeg
          ffmpeg -version | head -1

      - name: Stream Pokemon (Pluto TV) via RTMP with -c copy
        env:
          RTMP_URL: ${{ secrets.RTMP_URL }}
          STREAM_URL: ${{ secrets.STREAM_URL }}
        run: |
          RURL="${RTMP_URL}"
          SRC="${STREAM_URL:-https://jmp2.uk/plu-6683cd71a1d7ad000866ec6a.m3u8}"
          echo "==> مصدر البث: $SRC"
          echo "==> الهدف (Restream): $RURL"

          for i in $(seq 1 999999); do
            echo "----- محاولة البث رقم #$i -----"
            timeout 330m ffmpeg -hide_banner -loglevel warning \
              -re -i "$SRC" \
              -map 0:v:0 -map 0:a:0 \
              -c copy \
              -f flv -flvflags no_duration_filesize \
              "$RURL"
            echo "----- انقطع الاتصال، إعادة المحاولة بعد 5 ثوانٍ -----"
            sleep 5
          done
